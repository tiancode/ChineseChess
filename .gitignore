# Rust build output
/target

# Local game saves (user data)
/xiangqi_save.json
*.save.json

# Python bytecode cache (alphazero subproject)
__pycache__/
*.pyc

# AlphaZero model checkpoints: large local training artifacts (~24 MB each),
# regenerated by `python -m alphazero.pipeline`; not versioned.
/alphazero/checkpoints/
